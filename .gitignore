# ── 本机会话产物 / agent 注入（本机路径，不进版本库）──
.claude/
CLAUDE.local.md
AGENTS.md

# ── 构建与打包产物 ──
dist/
build/
framework/shipped/    # make package 把出厂件拷进包时的暂存，随包走不进 git

# ── 密钥与本地配置：只进环境变量或本地文件，绝不进代码 ──
.env
.env.*
!.env.example

# ── 系统与工具杂物 ──
.DS_Store
__pycache__/
*.pyc
.venv/
node_modules/

# ── 运行产物：工作区里跑出来的东西不进 git。样例项目进 git 的只有 project.md、需求（requirement.md / .lock 与它的存档）、
# 原件 materials/、流实例 flows/、设计那包 design/；实验及之后的产出、平台记录（含项目里的对话）都是本机的 ──
projects/*/.ai4sci/
projects/*/workspaces/*/.ai4sci/*
!projects/*/workspaces/*/.ai4sci/requirement/
projects/*/workspaces/*/literature/
projects/*/workspaces/*/hypothesis/
projects/*/workspaces/*/experiment/
projects/*/workspaces/*/analysis/
projects/*/workspaces/*/writing/
projects/*/workspaces/*/verification/
/studio/
*.egg-info/
