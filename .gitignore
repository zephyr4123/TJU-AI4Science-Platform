# ── 本机会话产物 / agent 注入（本机路径，不进版本库）──
.claude/
CLAUDE.local.md
AGENTS.md

# ── 构建与打包产物 ──
dist/
build/

# ── 密钥与本地配置：只进环境变量或本地文件，绝不进代码 ──
.env
.env.*
!.env.example

# ── 系统与工具杂物 ──
.DS_Store
__pycache__/
*.pyc
.venv/
node_modules/

# ── 运行产物：runs/ 里的东西按纲领不进 git ──
runs/
*.egg-info/
