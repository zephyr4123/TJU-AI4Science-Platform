# ── 本机会话产物 / agent 注入（本机路径，不进版本库）──
.claude/
CLAUDE.local.md
AGENTS.md

# ── 构建与打包产物 ──
dist/
build/

# ── 密钥与本地配置：只进环境变量或本地文件，绝不进代码 ──
.env
.env.*
!.env.example

# ── 系统与工具杂物 ──
.DS_Store
__pycache__/
*.pyc
.venv/
node_modules/

# ── 运行产物：工作区里跑出来的东西不进 git。样例工作区进 git 的只有需求（requirement.md / .lock 与它的存档）、
# 原件 materials/、流实例 flows/、设计那包 design/；实验及之后的产出、平台记录都是本机的 ──
workspaces/*/.ai4sci/*
!workspaces/*/.ai4sci/requirement/
workspaces/*/literature/
workspaces/*/hypothesis/
workspaces/*/experiment/
workspaces/*/analysis/
workspaces/*/writing/
workspaces/*/verification/
/studio/
*.egg-info/
