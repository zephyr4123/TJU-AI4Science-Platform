# 领域包：PEtab 参数估计工具链（pyPESTO + petab + libroadrunner）。
# 按工具链命名不按学科：平台看到的是"若干参数最小化一个 NLL"这个形状。
# 字段只收有读取点的：id 与 display_name（读取点在 framework/experiment/pack.py，只查文件在不在）。
# 领域包里放什么、怎么接一个新的：docs/add-a-domain.md。prompts/experiment.md 由 cap auto-research 开实验时
# 快照进产出目录、随执行层提示的「领域约定」段注入；skills/*/ 只按名字进执行层的清单，不快照、不注入正文。
id: petab
display_name: PEtab 参数估计
