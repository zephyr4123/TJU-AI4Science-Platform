"""工作区层：一份需求的家在盘上怎么摆（纲领 P-15、P-19）。

`project` 项目、`root` 工作区的根与七个阶段目录、`outputs` 产出目录的建与列、`progress` 一条流程
走到哪（沿 from 链算）、`jobs` 后台作业、`removal` 删。只依赖 contracts、skills 与两个端口（jobs
停作业要杀机器上的进程）；不认识能力、对话、页面。
"""
